const clock = require('./modules/clock');
const file = require('./modules/file');
const json = require('./modules/json');


module.exports = {
    clock,
    file,
    json
}
async function init(){

}